# Build Resources

Place your app icons here before running `electron-builder`:

- `icon.ico` — Windows installer icon (256×256 recommended)
- `icon.icns` — macOS app icon (generated from a 1024×1024 PNG using `iconutil`)
- `icon.png` — Linux (512×512 PNG)

These are not committed to the repository. Generate them from your school logo.
